/**
 * @deprecated Supabase removed in favor of self-host Postgres + Drizzle.
 * This file kept only to avoid build errors from stale imports.
 * Use `import { db } from '@/lib/db/client'` instead.
 */
export {};
