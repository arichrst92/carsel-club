/**
 * DEPRECATED — superseded by JoinPublicSessionButton.
 * Kept as a thin re-export so any lingering imports compile.
 */
export { JoinPublicSessionButton as JoinSessionButton } from "./JoinPublicSessionButton";
